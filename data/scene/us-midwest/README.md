# US Midwest hybrid scene tiles

This directory will contain the manifest for `scene-us-midwest-hybrid-v1`.
Tiles use the same 5 km offline OSM/SRTM format as the Northeast release and
are loaded on demand with local caching. Missing or unavailable scene data
falls back to live providers and finally the common heuristic tier.
